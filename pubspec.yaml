name: gangplank
description: Gangplank is a package designed to ease the use of the LCU (League client update) API. Provides a League client watcher, socket connector and HTTP client.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
      - assets/riotgames.pem
      - assets/example.jpg